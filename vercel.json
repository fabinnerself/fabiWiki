{
   "buildCommand": "python3 -m pip install --break-system-packages -r requirements.txt && python3 -m mkdocs build",
   "outputDirectory": "site",
   "framework": null,
   "rewrites": [
     {
       "source": "/(.*)",
       "destination": "/$1"
     }
   ]
}